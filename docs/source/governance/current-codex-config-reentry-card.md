# Current Codex Config Re-entry Card

## Purpose

This card is the current re-entry point for governed Codex-config work after the
failed broader config-governance attempt was rolled back.

Use it like this:

`I open this card -> I confirm the only active plan and the current minimal slice -> I run one bounded governance loop -> I write one result note or one bounded doc change -> I stop`

This file is not:

- a permission slip to recreate `codex-host-config/`
- a permission slip to reopen a large host-config promotion program
- a reason to widen into general OpenSpec surface cleanup again

## Current Intent

Return to Codex-config governance in the smallest safe shape:

- keep global config thin
- keep task strategy in OpenSpec cards
- repair one bounded governance gap at a time
- avoid direct live-target mutation until a narrow slice is frozen

## Current State

Known baseline for this re-entry:

- the failed current config-governance attempt was rolled back
- `openspec/config.yaml` is back to the thin stock version
- live `~/.codex/AGENTS.md` was restored from backup
- repo-global governance still exists as older baseline changes
- the older host-config projection change remains archived evidence, not a live
  program to recreate wholesale

## Active Plan

The only active plan for this window is:

- `govern-openspec-and-codex-devops-workflow`

Do not open a second active plan in the same window.

## Current Minimal Slice

The next safe slice is:

- define one reusable strategy-card-driven template for Codex-config governance
- freeze one current re-entry card for the next bounded config slice

This slice is repo-doc only.

## Not In This Slice

Do not widen this slice into:

- recreating `codex-host-config/`
- live apply into `~/.codex/config.toml`
- live rewrite of `~/.codex/rules/default.rules`
- fresh active-surface cleanup across unrelated OpenSpec changes
- `opencode-box` feature execution

## Execution Hook

Use:

- repo-doc editing only
- shell lookup only when a path, script, or existing archived evidence must be
  confirmed

Do not use:

- live host promotion
- VM103 execution
- broad multi-surface remediation in the same loop

## Output Artifact

This slice should end with:

- one updated reusable template
- one current re-entry card
- one task update that records this bounded governance slice

## Continue / Stop Gate

Continue only when:

- the next loop is still inside Codex-config governance
- the next loop still changes one narrow surface
- the next loop can still be explained in one sentence

Normal stop when:

- the current template and re-entry card are landed
- the next bounded slice is clear

Forced stop when:

- the next step requires recreating a full host-config source tree
- the next step requires live apply to `~/.codex`
- the next step starts mixing strategy, deployment, and broad cleanup at once

## Next Recommended Slice

After this card is landed, the next bounded slice should be only one of:

1. classify exactly which live Codex surfaces deserve durable governance now
2. define one minimal source layout that does not recreate the failed broad
   projection attempt
3. define one dry-run-only promotion path for one surface

Do only one of those in the next window.
