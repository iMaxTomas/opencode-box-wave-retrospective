## 1. Current-State Intake

- [ ] 1.1 Record the current host `Serena` project state, including onboarding status, available project memories, and the current root language coverage gap.
- [ ] 1.2 Record the current `RecallNest` shared-memory role in the host / `VM103` topology, including which surfaces already depend on it today.

## 2. Six-Agent Access Matrix

- [ ] 2.1 Define the allowed `Serena` access tier for each of the six current agents.
- [ ] 2.2 Define the allowed `RecallNest` access tier for each of the six current agents.
- [ ] 2.3 Record the ownership rule that host `Codex` and `VM103` `Codex` remain the primary semantic and continuity owners unless later executed evidence widens that scope.

## 3. Serena Project Strategy

- [ ] 3.1 Define the host-planning `Serena` surface and its target language coverage for this workspace.
- [ ] 3.2 Define the first subproject `Serena` candidates and the bounded-task rule for switching from host planning into a subproject semantic surface.
- [ ] 3.3 Record the rollout order for future `Serena` expansion so the project does not try to widen all subprojects at once.

## 4. Bounded Packet Contract

- [ ] 4.1 Define the `serena-anchor-bundle` packet contract.
- [ ] 4.2 Define the `recallnest-continuity-brief` packet contract.
- [ ] 4.3 Define who may produce, consume, review, and promote those packets across host, `VM103`, and the external lanes.

## 5. Readiness Alignment

- [ ] 5.1 Update the six-agent readiness line so each agent records approved `Serena` and `RecallNest` access modes.
- [ ] 5.2 Update multi-runtime rule/config readiness so future serious-task reviews include `Serena` coverage and `RecallNest` access strategy.
- [ ] 5.3 Record the first operator-facing next step for execution:
  - expand `Serena` project coverage first
  - run one bounded six-agent trial
  - or defer widening for config cleanup
